# eazy-gateway configuration
# Copy this file to config.yaml and adjust as needed.

# HTTP server listen address
# Default: :8022
listen_address: ":8022"

# Directory for persistent data (e.g., SSH host keys, user data)
# Default: ./data
data_directory: "./data"

# Log level: debug, info, warn, error
# Default: info
log_level: "info"
